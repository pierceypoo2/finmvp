/** Inline in root layout <head> — must not depend on any JS chunk (same key as old ChunkLoadRecovery). */
export const CHUNK_RECOVERY_SCRIPT = `(function(){var K='ta_chunk_reload_once';function ok(r){var s=typeof r==='string'?r:((r&&r.message)||'');var n=(r&&r.name)||'';return n==='ChunkLoadError'||s.indexOf('Loading chunk')!==-1||s.indexOf('ChunkLoadError')!==-1;}function go(r){if(!ok(r))return;try{if(sessionStorage.getItem(K))return;sessionStorage.setItem(K,'1');}catch(e){}location.reload();}window.addEventListener('error',function(e){go(e&&e.message);});window.addEventListener('unhandledrejection',function(e){go(e.reason);});})();`;
